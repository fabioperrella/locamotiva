<h2><%= t(:voting_for_next_month_races) %></h2>

<table class="table1">
<% @races.each do |race| %>
  <tr>
    <td>
      <%= render :partial => "shared/date_box", :locals => { :race => race } %>
    </td>
    <td class='title'>
      <%= link_to race.name, race.url, :target => "_blank" %>
    </td>
    <td>
      <%= race.distance %>
    </td>    
    <% if current_user %>
    <% #TODO: fazer link para votar %>
    <% end %>
  </tr>
<% end %>
</table>