<h1>Calendario de corridas</h1>
<table class="table1">
<% @races.each do |race| %>
  <tr>
    <td>
      <%= render :partial => "date_box", :locals => { :race => race } %>
    </td>
    <td class='title'>
      <%= link_to race.name, race.url %>
    </td>
    <td>
      <%= race.distance %>
    </td>    
    <% if current_user %>
    <td>
      <%= link_to t(:subscription).downcase, subscriptions_path(race), :method => :post %>
    </td>
    <% end %>
  </tr>
<% end %>
</table>